<h2 class='text-center welcome'>Bem vindo ao melhor compartilhamento de conhecimento.</h2>

<div class='row'>
  <% if current_user and !current_user.profile %>
    <h4>Você ainda nao completou o seu perfil, <%= link_to 'Clique aqui', new_profile_path %> para continuar</h4>
  <% end %>
</div>

<div class='row'>
    <%if user_signed_in? %>
      <%= link_to 'Propostas recebidas', my_proposals_path, class:'btn btn-success' %>
      <% if current_user.ads.where(status: :active).empty? %>
        <%= link_to 'Criar Anuncio', new_ad_path, class:'btn btn-success' %>
      <% end %>
    <% end %>
</div>

<div class='row'>
<% if @ads.empty? %>
  <div class="alert alert-info">
    <h4>Não temos anúncios por agora em breve novos anúncios para você. Volte logo!</h4>
  </div>
<% end %>
</div>

<div class='row'>
  <% @ads.each do |ad|%>
  <div class='col-xs-12 col-sm-4 col-md-4 col-lg-4'>
    <%= render ad %>
  </div>
  <% end %>
</div>
